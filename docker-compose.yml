version: '3'
services:
  strapi:
    container_name: portfolio.online
    build: .
    image: portfolio.online:latest
    restart: always
    env_file: .env
    volumes:
      - ./:/opt/app
    ports:
      - '5555:5555'
